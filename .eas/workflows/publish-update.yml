name: Publish update on push

'on':
  push:
    branches: ['main']

jobs:
  publish_update:
    type: update
    params:
      channel: preview
      message: 'Update published automatically from GitHub push'
